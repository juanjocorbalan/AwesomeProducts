import UIKit

public enum Styles {
    public enum Colors {
        public static let accentColor = UIColor(resource: .accent)
    }
    
    public enum BorderWidth {
        public static let thumbnail: CGFloat = 3.0
        public static let product: CGFloat = 1.0
    }
    
    public enum Paddings {
        public static let detailg: CGFloat = 32
        public static let list: CGFloat = 16
    }
    
    public enum CornerRadius {
        public static let regular: CGFloat = 16
    }
    
    public enum Layout {
        public static let productAspectRatio: CGFloat = 1.3
    }
}

